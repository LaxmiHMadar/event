.App {
  text-align: center;
}

.App-logo {
  height: 40vmin;
  pointer-events: none;
}

@media (prefers-reduced-motion: no-preference) {
  .App-logo {
    animation: App-logo-spin infinite 20s linear;
  }
}

.App-header {
  background-color: #343a46;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: calc(10px + 2vmin);
  color: white;
  padding-block: 0.5rem;
}

.App-link {
  color: #61dafb;
}

@keyframes App-logo-spin {
  from {
    transform: rotate(0deg);
  }

  to {
    transform: rotate(360deg);
  }
}

main {
  min-height: 80vh;
  padding-block: 2rem;
  padding-inline: 4rem;

  color: #414141;
}

input,
select,
button {
  padding: 6px 12px;
  color: inherit;
  font-size: inherit;
  border: 1px solid gray;
  border-radius: 6px;
}

button {
  border: 1px solid rgb(174, 174, 174);
  cursor: pointer;

  &:hover {
    background-color: rgb(227, 229, 230);
  }

  &:active {
    background-color: revert;
  }

  &.primary {
    border: none;
    background-color: #343a46;
    color: white;

    &:hover {
      background-color: #292e37;
    }

    &:active {
      background-color: #343a46;
    }
  }
}

.page-title {
  font-size: 20px;
  text-align: left;
  font-weight: bold;
}

.form-input {
  margin-block: 0.75rem;

  input,
  label,
  select {
    display: block;
    text-align: left;
  }

  label {
    font-size: 15px;
    padding-block: 8px;
  }
}

.form-row {
  display: flex;

  .form-input {
    flex-grow: 1;
  }
}

.action-bar {
  display: flex;
  justify-content: end;
  gap: 1rem;
  margin-block: 1rem;
}

table {
  width: 100%;
  text-align: left;
}body {
  font-family: Arial, sans-serif;
}

h1 {
  color: blue;
  text-align: center;
}

p {
  font-size: 20px;
}
/* App.css */

/* Global reset and body background */
body {
  margin: 0;
  padding: 0;
  font-family: Arial, sans-serif;
  background-color: #f0f0f0; /* Light gray background for the body */
}

/* App container */
.App {
  text-align: center;
}

/* Header background color */
.App-header {
  background-color: #4CAF50; /* Green background for the header */
  padding: 20px;
  color: white;
  font-size: 2em;
  font-weight: bold;
}

/* Main content styling */
main {
  background-color: #ffffff; /* White background for the main content */
  padding: 20px;
  min-height: calc(100vh - 150px); /* Adjust height for header and footer */
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Optional shadow for the main section */
}

/* Footer background color */


/* Link styles */
a {
  color: #61dafb;
  text-decoration: none;
}

a:hover {
  text-decofooter {
    background-color: #333; /* Dark gray background for the footer */
    color: white;
    padding: 10px 0;
    position: absolute;
    bottom: 0;
    width: 100%; /* Change this value */
    text-align: center;
    font-size: 0.9em;
    ration: underline;

  }
  
}


/* AddEvent.css */

/* Styling for the Add Event page container */
div {
  background-color: #f9f9f9; /* Light gray background for the container */
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Optional shadow for the form box */
  max-width: 600px;
  margin: 20px auto; /* Center the form */
}

/* Page title styling */
.page-title {
  font-size: 24px;
  font-weight: bold;
  color: #333;
  margin-bottom: 20px;
  text-align: center;
}

/* Form row styling */
.form-row {
  display: flex;
  justify-content: space-between;
  margin-bottom: 20px;
}

/* Individual input fields */
.form-input {
  flex: 1;
  margin-right: 10px;
}

.form-input label {
  display: block;
  margin-bottom: 5px;
  font-weight: bold;
  color: #555;
}

.form-input input {
  width: 100%;
  padding: 8px;
  border-radius: 4px;
  border: 1px solid #ccc;
  background-color: #fff; /* White background for input fields */
}

/* Action buttons */
.action-bar {
  margin-top: 20px;
  text-align: right;
}

.action-bar button {
  padding: 10px 20px;
  border-radius: 4px;
  border: none;
  margin-left: 10px;
}

.action-bar .primary {
  background-color: #4CAF50; /* Green background for submit button */
  color: white;
}

.action-bar button[type="button"] {
  background-color: #f44336; /* Red background for cancel button */
  color: white;
}


